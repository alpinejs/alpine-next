{
    "name": "alpine-next",
    "version": "1.0.0",
    "description": "The rugged, minimal framework for building a simpler web.",
    "author": "Caleb Porzio",
    "license": "MIT",
    "source": "src/index.js",
    "main": "dist/alpine.js",
    "dependencies": {
        "cypress": "^5.5.0"
    },
    "devDependencies": {
        "@vue/reactivity": "^3.0.2",
        "esbuild": "^0.8.39"
    },
    "scripts": {
        "build": "esbuild builds/index.js --bundle --outfile=dist/alpine.js --minify --define:process.env.NODE_ENV=\\\"production\\\"",
        "watch": "esbuild builds/index.js --bundle --outfile=dist/alpine.js --watch --define:process.env.NODE_ENV=\\\"production\\\"",
        "test": "cypress open"
    }
}
